server:
  port: 3040
  allowed-origins: "*"
datastore:
  system: /var/lib/fxdmx/db/system.db
  retentiondays: 30

# Local network discovery for fxcontroller.
discovery:
  enabled: true
  name: "" # Default: service-hostname-port; maximum 63 bytes
  id: "" # Set a unique stable ID to retain identity across hostname/port changes
